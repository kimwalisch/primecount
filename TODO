1) Create src/BitSieve.cpp
    * Inline functions must be as small as possible
    * Inline functions should use very few (<= 2) registers
    * Check all other inline functions

2) Update ./docs
    * write document about special leaves
    * write document about parallel S2(x, a)
